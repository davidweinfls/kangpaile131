typedef float FOO;
structdef MS {
	int a, b;
};

int x;
const float y = 55.5;
bool[4] z;
MS t;
function : void foo(bool[4] &p1, bool* p2) {
	x = sizeof(p1);			// 16 // bug
	x = sizeof(p2);			// 4 
}

function : void main() {
    foo(z, z);
    x = sizeof(FOO);		// should be 4
    x = sizeof(MS);			// 8
    x = sizeof(float***);	// 4	
    x = sizeof(int[3]);		// 12
    x = sizeof(x);			// 4
    x = sizeof(y);			// 4
	x = sizeof(z);			// 16
	x = sizeof(t);			// 8


	/* ERRORS */

	sizeof (2);
	
}